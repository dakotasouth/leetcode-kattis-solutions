import java.util.*;

class Solution {
    public class Node{
        int hgt;
        boolean seen;
        boolean flows;

        Node(int hgt){
            this.hgt = hgt;
            seen = false;
        }
    }
    public List<List<Integer>> pacificAtlantic(int[][] heights) {
        //create list of nodes
        //node: seen, integer, flows
        
        List<Node> nodes = new List<>();
        
        for(int i=0; i<heights.length)
        
    }
}